{
  "name": "parque-diversiones",
  "version": "1.0.0",
  "description": "Módulo de gestión de atracciones",
  "main": "app.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
